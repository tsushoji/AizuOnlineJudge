#include "0383.h"
/*============================================================================*/
/* global */
/*============================================================================*/

/*============================================================================*/
/* const */
/*============================================================================*/

/*============================================================================*/
/* prototype */
/*============================================================================*/
int main()
{
    int A, B, X;
	int a, b, tmpNum;
	scanf("%d %d %d", &A, &B, &X);
	if (A < B) {
		a = X / 1000;
		b = 0;
		if (X % 1000) {
			a++;
		}
	}
	else if (A > 2 * B) {
		a = 0;
		b = X / 500;
		if (X % 500) {
			b++;
		}
	}
	else {
		if (500 >= X) {
			a = 0;
			b = 1;
		}
		else if (1000 >= X) {
			a = 1;
			b = 0;
		}
		else {
			a = X / 1000;
			b = 0;
			tmpNum = X % 1000;
			if (tmpNum > 0 && tmpNum <= 500) {
				b++;
			}
			else if(tmpNum > 0 && tmpNum < 1000){
				a++;
			}
		}
	}
	int sum = a * A + b * B;
	printf("%d\n", sum);
    return 0;
}
